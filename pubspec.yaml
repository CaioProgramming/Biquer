name: Biquer
description: App to biqueiros that want to work with bico
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera:
  path_provider:
  path:
  cupertino_icons: ^0.1.3
  flutter_launcher_icons: ^0.7.5
  flutter_icons: ^1.1.0
  firebase_core: ^0.4.4+3
  firebase_auth: ^0.16.0
  cloud_firestore: ^0.13.5
  firebase_storage: ^3.1.6
  google_fonts: ^1.1.0
  google_sign_in: ^4.4.6
  url_launcher: ^5.4.7
  provider: ^3.0.0+1
  adobe_xd: ^0.1.0
  package_info: ^0.4.1
  image_picker: ^0.6.7+2
  mask_text_input_formatter: ^1.0.7
  flutter_svg: ^0.17.0
  email_validator: ^1.0.0
  cpf_cnpj_validator: ^1.0.5
  tinycolor: ^1.0.2
  flutter_statusbarcolor: ^0.2.3
  auto_size_text: ^2.1.0
  sleek_circular_slider: ^1.1.0

flutter_icons:
  android: true
  ios: true
  image_path: "C:/Users/cacai/Documents/Develop/UI Kits/biquericon.png"

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - images/
